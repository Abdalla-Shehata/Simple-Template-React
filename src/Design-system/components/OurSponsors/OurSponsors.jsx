import React from 'react'

export default function OurSponsors({ source, alternate }) {
    return (
        <div>
            <img width="100%" src={source} alt={alternate} />
        </div>
    )
}
